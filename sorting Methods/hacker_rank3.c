// in this algo I am reversing the array.
// in this i had useed revarced for loop.

#include <stdio.h>

// this function is to revarce the array by revercing the foor loop 

int main (){
char n;
scanf("%[^\n]", &n);

printf("%c\n", n);

    return 0;
}